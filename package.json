{
  "name": "ping-pong-js-game",
  "version": "3.2.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "npm run test:unit",
    "test:unit": "node --test",
    "test:coverage": "node scripts/coverage-gate.mjs",
    "test:e2e": "playwright test",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json",
    "check": "npm run lint && npm run typecheck && npm run test:coverage && node scripts/check-project.mjs",
    "docs:preview": "node scripts/capture-preview.mjs",
    "docs:icons": "node scripts/render-icons.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "eslint": "10.10.0",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=22.13"
  }
}
