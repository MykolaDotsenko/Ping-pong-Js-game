{
  "name": "ping-pong-js-game",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "npm test && node scripts/check-project.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
