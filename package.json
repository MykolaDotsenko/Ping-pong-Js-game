{
  "name": "ping-pong-js-game",
  "version": "2.3.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "npm run test:unit",
    "test:unit": "node --test",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-include=src/** --test-coverage-lines=95 --test-coverage-branches=90 --test-coverage-functions=90",
    "test:e2e": "playwright test",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json",
    "check": "npm run lint && npm run typecheck && npm run test:coverage && node scripts/check-project.mjs",
    "docs:preview": "node scripts/capture-preview.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "eslint": "10.10.0",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=22.13"
  }
}
